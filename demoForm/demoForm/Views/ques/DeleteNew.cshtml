@model demoForm.Models.queSet

@{
    Layout = null;
    ViewBag.Title = "DeleteNew";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DeleteNew</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>

        <dl hidden class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.AccountID)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.AccountID)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.QuesSet)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.QuesSet)
            </dd>

        </dl>

        @using (Html.BeginForm("DeleteNewConfirmed", "ques"))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <input hidden type="text" name="id" value=@ViewBag.id>
            </div>
                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-default" /> |
                    @Html.ActionLink("Back to List", "Index")
                </div>
                }
            </div>
</body>
</html>
